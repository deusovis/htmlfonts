name: Daily Typography SEO Automation

on:
  schedule:
    - cron: '0 8 * * *' # Executes every day at 08:00 UTC
  workflow_dispatch:      # Allows you to trigger it manually for testing

jobs:
  update-content:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # Crucial for the bot to push JSON updates

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-generativeai tweepy

      - name: Run Automation Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
        run: python automation.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add content.json
          git commit -m "Automated SEO update: New Typography Tip" || exit 0
          git push
